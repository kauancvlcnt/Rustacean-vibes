// println!("Your name");
// stdin().read_line(&mut user1.name).expect("error");
// println!("Your lastname ");
// stdin().read_line(&mut user1.lastname).expect("error");
// println!("Your age");
// stdin().read_line(&mut user1.age).expect("erro");
// println!("{:#?}", user1);

// (user1.name, user1.lastname, user1.age) = (
//     String::from(user1.name.trim()),
//     String::from(user1.lastname.trim()),
//     String::from(user1.age.trim()),
// );
